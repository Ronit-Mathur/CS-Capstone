import * as React from 'react'
import { Text, View, SafeAreaView, RefreshControl, Alert, Button, Pressable } from 'react-native'

function UnratedTasks (){
    return (
        <SafeAreaView>


        </SafeAreaView>
    );
}

export default UnratedTasks